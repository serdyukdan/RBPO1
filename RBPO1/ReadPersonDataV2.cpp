#include <iostream>
using namespace std;

void ReadPersonData(string& name, unsigned short &age, unsigned short &height, unsigned short &weight)
{
	cin >> name;
	cin >> age;
	cin >> height;
	cin >> weight;
}